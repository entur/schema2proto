syntax = "proto3";

message A {
    // Time individual response element was created.
    uint64 response_timestamp = 1;
}

message B {
    LangType lang = 1;
    string value = 2;

}

enum LangType {
    LANG_TYPE_UNSPECIFIED = 0;
    LANG_TYPE_A = 1;
    LANG_TYPE_B = 2;
}
