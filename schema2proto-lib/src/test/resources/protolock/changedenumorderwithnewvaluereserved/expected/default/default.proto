// default.proto at 0:0
syntax = "proto3";
package default;

enum Test {

  // Reservation added by schema2proto
  reserved 3;
  // Reservation added by schema2proto
  reserved "TEST_OLD";

  TEST_UNSPECIFIED = 0;
  TEST_SECOND = 2;
  TEST_NEW = 4;
  TEST_FIRST = 1;
}

