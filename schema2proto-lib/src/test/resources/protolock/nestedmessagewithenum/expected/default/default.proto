// default.proto at 0:0
syntax = "proto3";
package default;

message Wrapper {
  enum Test {
    // Reservation added by schema2proto
    reserved "TEST_THIRD";
    // Reservation added by schema2proto
    reserved 3;

    TEST_UNSPECIFIED = 0;
    TEST_FIRST = 1;
    TEST_SECOND = 2;
    TEST_FOURTH = 4;
  }

}
