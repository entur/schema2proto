//
syntax = "proto3";
package default;

enum Test {

  // Reservation added by schema2proto
  reserved 2;
  // Reservation added by schema2proto
  reserved "TEST_SECOND";

  TEST_UNSPECIFIED = 0;
  TEST_FIRST = 1;
  TEST_THIRD = 3;
}
